# 更新履歴

## 6.0.2

- **古い端末で開いただけで、他の端末の最新がサーバーから上書きされることがある不具合を直しました**
  - サーバーに保存しているものが増える（プロジェクト十数個ほど）と、一覧の読み込みが途中で切れ、
    サーバーにあるプロジェクトを「無い」と取り違えて、古い端末の内容で上書きしていました
  - 一覧の読み込みに失敗したときも、同じように上書きすることがありました
  - サーバーの内容を上書きするのは、送る直前にサーバーを確かめたときと、「この端末の方」を選んだときだけになりました
- 開いている間の定期的な確認で、サーバーの読み取り回数を減らしました（無料枠を使い切って同期が止まるのを防ぐため）

## 6.0.1

- **空き容量が足りないとき、書いた内容が消えることがある不具合を直しました**
  - サーバーの内容で置き換えるときや、復元ポイントから戻すとき、今の内容の控えを先に残します。
    6.0.0 では、空き容量が足りず控えを残せなくても、そのまま置き換えていました（控えにも残らず、知らせも出ませんでした）
  - 控えを残せたことを確かめてから置き換えます。残せないときは置き換えを止め、画面上部で知らせます。そこから「ファイルへ保存」で書き出せます
  - 6.0.0 の「選ばなかった方も復元ポイントに残ります」は、空き容量が足りないときには成り立っていませんでした
- 「今の状態を残す」で控えを残せなかったときも知らせるようにしました

## 6.0.0

- **データをサーバーに保存するようにしました**（クラウド同期を設定している場合）
  - すべてのプロジェクトがサーバーに保存され、どの端末で開いても同じ内容になります
  - 電波が無いあいだはその端末に控えておき、つながったら送ります。これまでどおりオフラインでも使えます
  - 新しい端末で開くと、サーバーにある最新のプロジェクトがそのまま出ます
  - プロジェクトごとに「サーバーに置かない」を選ぶこともできます
- **画像の多いプロジェクトも、画像ごと保存できる**ようになりました
  - これまでは大きいと画像を除いて共有していましたが、分けて保存するので欠けません
- **2つの端末で同時に編集したとき、黙って上書きしなくなりました**
  - 「サーバーの方」「この端末の方」のどちらを残すか選べます
  - 選ばなかった方も、その端末の復元ポイントに残ります
- **HTML 1枚で使える版**を作れるようになりました（`npm run single`）
  - ダブルクリックで開くだけで、インストールも exe も要りません

## 5.10.0

- **スマホ版が新しい版に更新されない不具合を直しました**
  - ホーム画面に追加したスマホ版が、最初に入れた版のまま固定され、
    パソコン側を新しくしても切り替わらない状態でした
  - 直したあとは、開くたびに裏で新しい版を確認し、次に開いたときに切り替わります
  - 電波が無いときにすぐ開ける点はこれまでどおりです
- スマホ版の**上部に版を表示**しました。今どの版が入っているか、開かなくても分かります

## 5.9.0

- 付箋の**番号(#)を「今ある付箋の通し番号」に変えました**。飛び番号がなくなります
  - 付箋を剥がすと、それより後ろの番号が繰り上がります（#3 を消すと #4 が #3 になります）
  - 本文に書いた `#5` のような参照も一緒に書き直されるので、**同じ付箋を指したままです**
  - ❝引用ボタンで付けた引用・線・入れ子・タグは、これまでどおり影響を受けません
  - 今までのデータにある飛び番号も、次に開いたときに詰められます
  - 注意: 番号は「今の並びの何番目か」を表すものになりました。紙などに控えた番号は、
    付箋を剥がすとずれることがあります

## 5.8.0

- **タグ表をクラウドで共有**するようにしました
  - どの端末でも同じタグ・カテゴリ・色を使えます
  - 開くたびにクラウドから読み直し、編集するとすぐ反映されます
  - 設定画面／タグ編集画面の「読み直す」で、手動でも取り直せます

## 5.7.0

- 届いた付箋を**見てから選んで貼れる**ようになりました。いらないものは貼らずに消せます
- スマホ版から**送り先のプロジェクトを選べる**ようになりました。パソコン側でそのプロジェクトを開いていると、最初から選ばれた状態になります
- スマホ版「受信箱」の呼び名を「**アイデアボックス**」に変えました
- スマホ版で同じ案内が2回出る不具合を直しました
- スマホ版で、タグ表とまとめ先が次に開いたときに戻らない不具合を直しました

## 5.6.0

- アプリの中に「**使い方**」を入れました（上部のボタンから開けます）
- 他の人に配るための一式を用意しました
  - `配布テンプレート/はじめにお読みください.txt` … アプリと一緒に渡す説明書
  - `docs/DISTRIBUTE.md` … 配布用ファイルの作りかた

## 5.5.0

- 付箋や文字を選んで **Delete / BackSpace キーで剥がせる**ようになりました
- 「＋ 追加」メニューの一番下に「**剥がすときに確認する**」のチェックを追加しました（外すとすぐ消えます）
- スマホ版の**タブが表示されない不具合を直しました**（「ためた分」が開けず、書き出しもできない状態でした）
- Macでの起動を速くしました
  - ビルドせずに起動する `npm run app` を追加（`npm run start` は毎回ビルドするため時間がかかります）
  - Apple SiliconのMacでIntel版を入れると非常に遅くなる点を docs/MAC.md に記載

## 5.4.2

- かんたん設定の案内を、現在のFirebaseの画面に合わせて直しました
  - Firestoreの場所を「Database と Storage」の中と明記
  - ウェブAPIキーの探しかたを「マイアプリ → SDK の設定と構成」と具体的に記載

## 5.4.1

- かんたん設定で、Firebaseの**各画面を直接開ける**ようにしました（メニューの場所が変わっても迷いません）
- 表記の揺れに合わせて案内を調整しました（「プロジェクトを作成／追加」「ログイン方法」など）

## 5.4.0

- クラウド同期の**設定をアプリ内で行えるようになりました**（「デバイス接続」→「かんたん設定をはじめる」）
  - 5つの手順を画面の案内どおりに進めるだけで設定できます
  - Firebaseの画面をアプリから直接開けます。貼り付ける内容もボタン1つでコピーできます
  - 「つないでみる」を押すと、**どこでつまずいているかをアプリが判別**して案内します
- 他の端末の設定を貼り付けて、すぐに使い始められるようにしました

## 5.3.1

- 「パソコン同士で共有」の表示を整理しました（成功時のメッセージをやめ、ボタン名を「同期する」に、状態表示を改行）

## 5.3.0

- **パソコン同士でプロジェクトを共有**できるようになりました。付箋の位置もそのまま再現されます
  - 別のパソコンにしかないプロジェクトは、起動画面や「デバイス接続」から取り込めます
  - 他のパソコンで更新があると知らせが出ます
  - ※ Firebaseのルールに `projects` の3行を足す必要があります（docs/CLOUD.md 参照）
- スマホ版: 「**まだ送っていない**」と「**これまで全部**」を切り替えて見られるようになりました
- スマホ版: 各付箋に送信済みかどうかを表示するようにしました

## 5.2.0

- 付箋を別のボードへ**移動**できるようになりました（これまでは複製のみ）
- 付箋一覧（☰）から**複数選択**して、複製・移動・剥がすをまとめて行えます
- 付箋一覧に、**どの領域の中にあるか**を表示するようにしました
- 同じWi-Fiで直接つなぐ機能と6桁番号を廃止し、**クラウド同期に一本化**しました
- スマホ版:「クラウド同期をやめる」で確認を出すようにしました
- スマホ版: **i ボタンから使い方**を見られるようにしました

## 5.1.0

- 起動時のプロジェクト選択画面に「開く」ボタンを追加。選んでから開く形になりました（ダブルクリックでも開けます）

## 5.0.0

これまでの機能をひととおり揃えた版です。

- スマホ版: 付箋を選んで共有・CSV出力・削除できるように
- スマホ版: 並び替え（↑↓）に対応。並べた順でパソコンに取り込まれます
- スマホ版: 長い文章が画面からはみ出さないように
- スマホ版: ホーム画面アプリとして開いた場合は、設定を保持できないためクラウド同期を使えないように変更
- 「データでコピー」を廃止し、CSV出力に置き換え
- 版の表示を追加
