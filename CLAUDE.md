# IdeaBoard - プロジェクトメモ

付箋型アイデアボードのアプリ。Electronデスクトップアプリ（Windows exe）＋ iPad/ブラウザ向けPWAの両対応。

## 構成
- `src/IdeaBoard.jsx` : アプリ本体（単一コンポーネント）。保存はlocalStorage
- `electron/main.cjs` : Electronメインプロセス
- `public/` : PWA用（manifest / sw.js / アイコン）
- `npm run start` : デスクトップアプリ起動 / `npm run dist` : Windowsポータブルexe作成
- `npm run dev` / `npm run build` : ブラウザ・PWA用（iPad手順は IPAD.md）

## 仕様（ユーザーの要望で決定済み）

### 付箋
- 貼る/剥がす/ドラッグ/右下角でリサイズ（収納時と展開時のサイズは別々に記憶）/色は開閉式パレット
- 傾きなし（xy軸に整列）。自由配置/グリッド配置(既定26px)の切替
- ドラッグのつかみ代は操作列の「ボタン以外の余白」。**操作列の行自体にdata-nodragを付けないこと**（シンプル枠モードでつかむ場所が消えるバグの原因）。除外は各ボタン・ポップオーバーに個別付与
- 未記入プレースホルダーは「アイデアを書く」のみ
- タイトル: `note.title`（1行。本文の上の入力欄）。表示用の見出しは `headingOf(n)` = タイトル → 空なら本文の最初の1行。俯瞰表示・引用プレビュー・引用ピッカー・タグ一覧・接続案内はすべてこれを使う（本文を直接参照しない）。引用検索はタイトルも対象
- 複製・コピー(⧉): 入れ子の中身・タグ・サブツリー内の線ごと複製。同一ボードは+24pxずらし＆外部引用も維持。他ボード/他プロジェクトはルート配置＆番号再採番、外部引用は破棄

### 入れ子
- 付箋の中にボードを格納、3段階まで（MAX_DEPTH=2）。親子（格納関係）同士は線で接続不可

### 線
- 付箋の枠と枠を**直線**で結ぶ（たわみなし）
- 見えているノード同士を直接つなぐ（展開中は 子→別親 を直接。**親を経由させない**：ユーザー指定）
- 収納されている側だけ、見えている親に束ねてオレンジ点線で表示
- 線のメモ: `edge.memo`。線クリックでメモ入力ポップアップ（完了/線を外す）。メモは中央に白ラベル表示、ラベルクリックで再編集（線クリック=即削除ではない）

### ID・引用・タグ
- ID(#番号)は自動採番。引用は「❝引用→検索(タグ/本文/番号)→リスト選択」。本文に `#番号` でも可
- 引用先のトップを付箋下部に表示、クリックでジャンプ（収納中は祖先を自動展開）
- タグ: 自由入力＋プリセット（カテゴリ別）。＋タグのプリセットチップは**トグル式**（押すたび付け外し）
- プリセット既定: シーン=起承転結/メイン/サブ/深掘り/描写/伏線1-2、設定=キャラクター/世界観/ギミック、アイデア=重要度高中低/実装/未実装、その他=済/未/優先度高中低
- タグはカテゴリごとに色分け（cat.color、既定 シーン=青/設定=紫/アイデア=橙/その他=緑、自由タグ=グレー）。色はチップ・強調枠・タグ一覧・引用ピッカーすべてに適用
- 赤色強調タグ: `settings.alertTags`（既定 重要度高・優先度高・未・未実装）はカテゴリ色より優先して赤(#C6392B)。付箋上で**常に先頭表示され、+n格納の対象にならない**
- 付箋上のタグ表示は `settings.noteTagLimit` 件まで、超過分は「+n」チップに格納（展開状態は非保存）
- タグ一覧(🏷): ボード内の全タグを使用数つき→タグ選択で該当付箋一覧→ジャンプ／強調

### ボード上の要素
- 直書き文字: 「＋文字」。ホバーで⠿(移動)/A(サイズ3段階)/✕(削除)。@media (hover:none)では常時表示
- 領域(＋領域): ドラッグで作成する色付き矩形。付箋の下(zIndex:0)、本体はpointerEvents:noneで操作を邪魔しない。操作は左上の名前バー（⠿移動・領域名・●色・✕削除）と右下ハンドル。**領域を動かすと領域内のアイテム（中心が領域内の最上位付箋・領域内の文字）も相対位置を保って一緒に動く**
- 画像: `board.images`。「＋画像」/ Ctrl+V / ドラッグ＆ドロップで追加。取り込み時にcanvasで長辺IMAGE_MAX_PX(1400)へ縮小（localStorage容量対策）。領域の上・付箋の下。縦横比を保つリサイズ
- 手書き(✏️): `board.strokes` に {id, pts, color, width}。モード中は全面入力レイヤー(zIndex 115000)、表示は110000（付箋より上）。Apple Pencilの筆圧(e.pressure)で線幅、中点二次ベジェで平滑化。下部バーで色5・太さ3・消しゴム・↩︎・全消し・「指＋ペン/ペンのみ」。上部で表示/非表示
- 消しゴムは2モード: "part"=なぞった部分だけ消して線を分割 / "whole"=触れた線ごと消す

### 表示・選択
- ズーム 30〜200%: −/＋、Ctrl(⌘)+ホイール、iPadは2本指ピンチ
- 俯瞰表示: `zoom < settings.overviewZoom`(既定0.65)で最上位付箋を「見出し(本文1行目・最大4行)＋タグ＋入れ子数」に切替。逆スケール(1/zoom)で文字の見かけの大きさを保つ。俯瞰中は操作列・タグ行・引用を非表示
- 俯瞰の見出しは付箋の枠外にはみ出す横長ラベル（幅 LABEL_W = clamp(170〜240px相当)、逆スケールで見かけサイズ一定、付箋色の縁＋影付き、2行クランプ）。付箋ルートに overflow:visible を付けてはみ出しを許可
- 俯瞰時のタグは**中身が分かるものを優先**: カテゴリの `overview` フラグ(既定 シーン/設定=true、アイデア/その他=false)で優先度を決め、優先カテゴリ→その他の順に最大3件＋残数。自由入力タグは優先側。**赤色強調タグの優先はここでは適用しない**（進捗・重要度より内容を優先：ユーザー指定）。設定画面のカテゴリ行「俯瞰で優先」トグルで変更可
- 見た目切替: 「付箋」/「シンプル」（白地に色枠）
- 複数選択: 空きスペースのドラッグで範囲選択（最上位の付箋＋直書き文字が対象）。Shift+クリックで個別追加/除外。まとめて移動、下部バーで一括複製/削除/解除。タッチは「▨ 選択」モードのときだけ範囲選択（通常はスクロール優先）

### プロジェクト・保存
- プロジェクト（ボード一式）を複数保持。📁メニューで名前変更・切替・新規・削除、JSONの書き出し/読み込み
- データ構造: `{projects:[{id,name,boards,currentBoardId}], currentProjectId, gridMode, zoom, simpleStyle, settings, zMax}`。旧形式は自動移行
- 自動保存は0.8秒デバウンス。**ブラウザ標準のconfirm/alertは使わない**（サンドボックスでブロックされる）。自前の`confirmBox`を使う
- ~~iPadとWindowsの自動同期は行わない方針~~ → **6.0.0 でユーザー判断により変更**。クラウド同期を設定していれば全プロジェクトをサーバーに保存する（下記 6.0.0 を参照）。クラウド未設定なら従来どおり端末内のみ

### 設定画面(⚙)
- グリッド間隔・キャンバスサイズ・付箋のタグ表示数・見出し表示に切替わる倍率(%)・文字サイズ3段階・赤色強調タグ・プリセットタグ（カテゴリの追加/削除/名前/色/タグ編集）
- 入力は**onBlur確定方式**（onChangeで分割すると日本語の区切り入力が壊れるため）
- 付箋リスト(☰): ツールバー左端のハンバーガーで開閉するサイドパネル(幅290px)。ボードと横並びで同時に見られる（fixedのオーバーレイではなくflexの兄弟要素）。検索(タイトル/本文/タグ/番号)、タグ絞り込み、「このボード/全ボード」切替、件数表示。行クリックでjumpTo（別ボードならswitchBoard後に少し遅らせてjumpTo）。入れ子の親や所属ボードも行内に表示
- Mac対応: electron/main.cjs は process.platform==='darwin' で分岐。**macでは標準メニュー(appMenu/editMenu等)を必ず残す**（Menu.setApplicationMenu(null)にすると⌘C/⌘V/⌘Qが効かなくなる）。titleBarStyle:'hiddenInset'。ビルドは `dist:win`(Windows上) / `dist:mac`(Mac上)。macは未署名(identity:null)なので初回は右クリック→開く。手順は MAC.md
- 付箋の既定色: COLORS末尾に「グレー」を追加し DEFAULT_COLOR=COLORS.length-1。**追加時はランダムではなく常に灰色**（既存データのcolorインデックスを壊さないため末尾に追加している）
- 「設定して貼る」モード(propMode): ONのとき addNote は pendingNote をセットしてモーダルを出し、色・タイトル・タグを決めてから addNoteNow で貼る。OFFなら従来どおり即座に灰色で貼られる。copySubtreeTo は addNoteNow を経由しないので影響なし
- 付箋のポップオーバー(色/タグ/引用/コピー)を開くときは bringToFront(id) でzを最前面に上げる（data-nodragで早期returnするためドラッグ時のz上げが効かず、他の付箋の下に隠れていた）
- 画像のz順は配列インデックス(1+imgIdx)で決める。zRefを付箋と共有すると画像が付箋より前面に出てしまうため。つかんだ画像は配列末尾へ移動して最前面に
- 領域(zone)コンテナは zIndex:0 でスタッキングコンテキストを作るため、色メニューを開いている間だけ 100003 に上げる
- タイトル欄は付箋ごとの `note.useTitle` で表示/非表示。操作列の「T」トグルで後から切替可。貼るポップアップの「タイトルを付ける」チェックが初期値。既存データは title が入っていれば useTitle=true として移行
- propMode（貼る前のプロパティ設定）は**既定ON**で、保存データに含める
- モーダルは枠外クリックで閉じる（設定画面／貼るポップアップ）。確認ダイアログは誤操作防止のため対象外
- 保存先アクセスは storeGet/storeSet に集約（アプリ版への変換はこの2関数の置換のみ）
- **変更点や仕組みの説明文を画面に足さない**（ユーザー指定）。変更点は md（CHANGELOG / docs / CLAUDE.md）に書けば十分。画面に出すのは操作に必要な文（確認・選択の問いかけ・失敗の知らせ）だけ
- 復元ポイント: BACKUP_KEY に最大BACKUP_MAX(6)件のスナップショットを保存。自動保存時にBACKUP_INTERVAL_MS(10分)以上空いていれば追加。容量超過時は古いものから捨てて再試行。📁メニューの「復元ポイント」から日時を選んで復元、復元前の状態も自動で1件残す。「今の状態を残す」で手動追加も可
- 保存はプロジェクト単位: INDEX_KEY(一覧＋共通設定) と projKey(id)(プロジェクト本体) に分割。自動保存では**中身が変わったプロジェクトだけ**書き込む(savedRefで比較)。旧 STORAGE_KEY は読み込み時に分割移行するだけ
- 復元ポイントも backupKey(projectId) でプロジェクトごと。プロジェクト削除時は projKey/backupKey も storeDelete する
- 起動時のプロジェクト選択画面(startOpen): 既存プロジェクトが2件以上のときに表示。新規作成・既存を開く・ファイルから開くを選べる。📁メニューの「プロジェクト選択画面を開く」でいつでも再表示できる
- スマホ連携(ローカル): electron/server.cjs が LAN内にHTTPサーバー(ポート3777)を立て、dist/mobile.html を配信。PIN(6桁・起動ごと生成)で端末を認証し、トークンを発行。POST /api/notes で受け取った付箋は main 側のキューに溜まり、PCのツールバー「📱 スマホ」から現在のボードへ貼る。preload.cjs の ideaboardBridge 経由(IPC)。Web/PWA版では bridge が無いのでUIごと非表示。手順は PHONE.md
- Viteは2エントリ構成: index.html(PC) と mobile.html(スマホ)。スマホUIは src/MobileQuickAdd.jsx
- スマホ版は独立PWA: public/mobile-manifest.webmanifest と public/mobile-sw.js により、mobile.html 単体でホーム画面追加・オフライン起動できる。distをNetlify等に置いて /mobile.html を開く運用（手順はPHONE.md）
- スマホ→PCの送信先は HOST_KEY に保存（別オリジンから送るため server.cjs に CORS と OPTIONS 対応を追加）。PCサーバーから開いた場合は location.origin が初期値になる
- ヘッダー/フッターは env(safe-area-inset-*) を CSS変数 --safe-t/--safe-b で参照（ホーム画面起動時に端が隠れないように）
- PC間共有: electron/sync.cjs が同期フォルダ(OneDrive等)に `<projectId>.ideaboard.json` を読み書きする。クラウド転送はサービス側任せ。書き込み前に mtime を比較して競合を検出し、画面上部で「読み込む/こちらを残す」を選ばせる。15秒ごとに一覧を確認し、別端末(device名)の更新を検知。同期対象プロジェクトは端末ごとに idea-board-synced に保存。手順は SYNC.md
- **Service Worker はブラウザ(http/https)専用**。file:// のデスクトップ版で登録されるとfetchを横取りして画面が真っ白になる（"Not allowed to load local resource" が出る）。index.html / mobile.html では protocol を見て登録を分岐し、file:// では既存の登録とキャッシュを解除する。さらに main.cjs 起動時に session.clearStorageData で serviceworkers / cachestorage を掃除している
- ツールバー整理: 「＋ 追加 ▾」(付箋/文字/画像/領域＋「貼る前に色・タグを決める」チェック)と「表示 ▾」(配置モード/見た目/範囲選択/手書き表示)にまとめ、盤面を広く保つ。ズームと手書き(✏️)は頻用のため単独ボタンのまま。モード中(領域作成/範囲選択)はツールバーに黄色の案内と✕を出す
- 色の運用メモ: settings.colorNotes[]（色ごとの使い分け）。設定画面で編集。settings.showColorNotes で表示/非表示を切替でき、OFFなら色パレットは丸だけの横並びに戻る
- スマホ版: ⚙から「PCとの接続」「オフライン対応の状態表示」。受信箱を空にするは赤ボタン＋確認モーダル
- macの署名: package.json の mac に identity:null を**書かない**（書くと署名が完全に省かれ、Gatekeeperの警告が最も強く出る）。未指定なら electron-builder が ad-hoc 署名を付ける。hardenedRuntime:false / gatekeeperAssess:false。それでも未公証のため初回は右クリック→開く、またはシステム設定から許可、最終手段は xattr -dr com.apple.quarantine（MAC.md に記載）
- mac のウィンドウUI対策: titleBarStyle:'hiddenInset' + trafficLightPosition で信号機ボタンを配置し、レンダラ側は bridge.platform==='darwin' のときツールバーに padding-left:84px を入れて重なりを防ぐ。**ツールバー全体に -webkit-app-region:drag を付けてはいけない**（その領域はクリックが届かず、メニューが開かなくなる）。ドラッグ可能にするのは左端80pxの余白だけ。ツールバーの常時ヒント文は削除し、アイコン化（🗑/📱/⚙）で横幅を詰めて折り返しを防ぐ
- **版の不一致はPC版に寄せて解決する**: スマホは色を「番号＋色名＋色コード」の3点セットで送り、PC側の resolveColor() が 色名→色コード→近い色→番号→既定色 の順で照合して**このPCの色定義に変換**して取り込む。並び順が変わっても名前を変えても正しい色になる。タグは文字列なのでそのまま通る
- DATA_VERSION（electron/server.cjs と src/MobileQuickAdd.jsx）は不一致の**通知用**。色やタグの定義を変えたら両方上げる。食い違うとPC側(📱パネル)とスマホ側(上部)に「PC版の設定に合わせて取り込まれます」と案内が出る
- ツールバーのラベルはアイコンのみにしない（見落とされる）。「デバイス接続」「設定」「タグ一覧」「空にする」はテキスト表示。デバイス接続ボタンは bridge が無い環境（ブラウザ）でも表示し、パネル内で「デスクトップ版で使えます」と案内する
- **起動の高速化**: 起動時は INDEX_KEY のメタ情報(projectMetas)だけ読み、**前回開いていた1プロジェクトの本体だけ**読み込む。他は開くときに ensureProject() で遅延読み込み。画像を含む全プロジェクトを毎回parseしていたのが遅さの主因だった
  - 一覧UI(起動画面・📁メニュー・コピー先)は allMetas（メタ＋読み込み済みの最新）を使う。projects には読み込み済みのものしか入っていない点に注意
  - sync.cjs の list() は mtime/size でキャッシュし、変化したファイルだけ読み直す（クラウド上のファイルを毎回全部開くと非常に遅い）
  - main.cjs の起動時 clearStorageData は削除（SW解除は index.html 側で実施）
  - ポーリング間隔: デバイス接続の状態はパネル表示中2秒／有効時8秒／それ以外30秒。同期フォルダは60秒、起動直後は1.2秒遅らせる
- テキスト書き出し: ツールバー「書き出す」、選択バー「📄 テキスト」、付箋の⧉「📄 テキストをコピー」。タイトルを使っている付箋は【タイトル】行＋本文、使っていなければ本文のみ。中の付箋はインデントで表現。タグ・番号・入れ子の有無をチェックで切替、コピーと.txt保存が可能
- スマホ版のタグ表は編集可能（CATALOG_KEY に保存）。タグ・カテゴリの追加/削除/並び替え（↑↓）/名前変更を「タグを編集」画面で行う。送信時に tagCatalog と、各付箋の主カテゴリ（最初に一致したカテゴリ名）を一緒に送る
- PC側の受信: tagCatalog を settings.presetTags にマージ（同名カテゴリはタグを統合、新規カテゴリは追加）。付箋はカテゴリごとにグループ化し、**カテゴリ名の領域(zone)を作ってその中に3列グリッドで配置**。領域は横に並べ、1500px を超えたら折り返す。重ならず、ほどよくまとまった形になる
- **大きなブロックを丸ごと置換するときは要注意**: receiveNotes を書き換えた際、範囲の取り方を誤って同期フォルダのセクション（syncInfo / syncNotice / pushProject / pullProject など）を巻き込んで削除し、起動時に `syncNotice is not defined` で白画面になった事故がある。置換後は必ず `npm run check`（tools/check.mjs）で定義漏れを確認する。build / dist からも自動で走る
- **https のスマホ版から http のPCへは送れない**（ブラウザの混在コンテンツ制限）。Netlify等に置いた版をホーム画面に追加している場合、PCへの直接送信は仕組み上できない。スマホ側で blockedByHttps を判定して案内を出し、代替として「データでコピー」→ PCの「デバイス接続」→「貼り付けて取り込む」経路を用意した
  - 直接送信を使いたい場合は、同じWi-Fi内でPCのアドレス（http://192.168.x.x:3777）をSafariで開いて送る
- クラウド同期（受信箱のみ）: src/cloud.js が Firestore REST API を直接叩く（SDK不使用・依存追加なし）。設定は {projectId, apiKey, room} を localStorage に保存。匿名認証でidTokenを取得（refresh_token で更新）。データは inbox/{room}/items に「payload = JSON文字列」1フィールドで格納
  - スマホ: 「受信箱に入れる」時に即送信。失敗分は unsent に積み、online イベントと手動ボタンで再送
  - PC: 60秒ごと（パネル表示中は8秒）に取得し、「◯件をこのボードに貼る」で取り込み＆クラウドから削除
  - 準備手順は CLOUD.md（Firestoreのルールと匿名認証の有効化が必須）
- クラウド設定の保存: loadCloudConf/saveCloudConf に加えて loadCloudDraft/saveCloudDraft（入力途中の控え）を用意。**読み込みは他の初期化処理と分けた独立の useEffect** で行う（まとめて読むと、途中で例外が出たときに設定だけ復元されない事故になる）。saveCloudConf は保存可否を返し、失敗時はその旨を画面に出す
- 注意: Safari で開いた場合とホーム画面アプリとして開いた場合で、保存先が別になることがある。設定は開き方ごとに1回ずつ必要になる場合がある（「設定をコピー」「設定を貼り付け」で移せる）
- スマホ版の「まとめ先」(AREAS_KEY): 付箋ごとに指定できる区分。名前と色を持ち、送信時に area / areaColor として渡す。PC側の placeIncoming は **area を最優先**（無ければタグのカテゴリ）でグループ化し、その色で領域(zone)を作る
- 版管理: src/version.js の APP_VERSION と package.json の version を**必ず両方**更新し、CHANGELOG.md に1行足す。現在 6.1.0。設定画面（PC）と⚙（スマホ）に表示される
- スマホ版の一覧: チェックで選択 → 共有・CSV出力・削除・PCへ送信は「選んだものだけ」（未選択なら全部）。↑↓で並び替えでき、**その順番のままPCに取り込まれる**（items は上から古い順で保持し、追加は末尾に足す）
- スマホ版の「データでコピー」は廃止し CSV 出力に置き換え（BOM付き・引用符エスケープ。navigator.share のファイル共有を優先、不可ならダウンロード、それも不可ならコピー）
- **ホーム画面アプリ（standalone）ではクラウド同期を出さない**: iOSのPWAは設定を保持できないことがあるため。判定は display-mode:standalone / navigator.standalone。CSVや共有での受け渡しを案内する

## 5.2.0 での変更
- **Wi-Fi直結（LANサーバー・6桁PIN）は廃止**。electron/server.cjs を削除し、preload から share:* を除去。スマホ→PCはクラウド同期（src/cloud.js）に一本化。届かない場合の手段は「CSV/共有でコピー → PCの『貼り付けて取り込む』」
- 付箋の移動: moveSubtreeTo / moveToProject（複製してから元を消す）。⧉メニューは各行に「複製」「移動」の2ボタン
- 付箋一覧（☰）から複数選択 → 複製/移動/剥がす。対象は**今のボードの付箋のみ**（他ボードの行にはチェックを出さない）
- 一覧に領域を表示: zoneOfIn(board, note) で「中心が入っている領域」を判定（重なりは面積の小さい方を優先）
- スマホ版: i ボタンからヘルプ（helpOpen）。「クラウド同期をやめる」は confirmStop で確認

## 5.3.0 での変更
- PC間のプロジェクト共有: cloud.js の projectPush/projectList/projectPull/projectRemove。`projects/{room}/items/{projectId}` に payload(JSON文字列)＋name/device/savedAt を保存。**Firestoreのルールに projects の match を足す必要がある**（既存ユーザーは貼り直しが必要）
  - 1ドキュメント約1MBの上限があるため、900KBを超えたら画像を除いて保存し、それでも超えたら失敗を返す
  - 共有中のプロジェクトは編集の8秒後に自動アップ。一覧は mask で name/device/savedAt だけ取得（本体を毎回取らない）
  - 他端末の更新は savedAt の比較で検知し、パネルに読み込みボタンを出す
- スマホ版: 「まだ送っていない」「これまで全部」の切替（listFilter）。各付箋に未送信／送信済みを表示

## 5.4.0 での変更
- クラウド設定をアプリ内のウィザードで行えるようにした（setupOpen / setupStep の5段階）。exe だけ配っても、受け取った人が自分で用意できる
  - electron 側に `open:external`（shell.openExternal）を追加し、Firebaseコンソールをアプリから開ける
  - cloud.js の cloudDiagnose() が失敗理由を段階で返す（key / auth / rules / firestore / net）。それに応じて「手順Nに戻る」ボタンを出す
  - FIRESTORE_RULES を cloud.js に持たせ、ウィザード内でコピーできるようにした（ドキュメントとコードで内容がずれないように）
  - 合言葉は makeRoomKey() で自動生成（20文字）。ユーザーに考えさせない

## 5.5.0 での変更
- Delete / BackSpace で選択中の付箋・文字を剥がす（入力欄にフォーカスがあるときは無効）
- settings.confirmDelete（既定 true）。askThen() が確認の有無を切り替える。チェックは「＋追加」メニューの最下部
- **スマホ版のタブが消えていた不具合を修正**。Wi-Fi機能を削除したとき、PIN入力バーと一緒にタブUIまで消していた。範囲削除のあとは必ず画面の主要素が残っているか確認すること
- macの起動が遅い件: ①`npm run start` は毎回 vite build が走る → `npm run app` を追加 ②Apple SiliconにIntel版dmgを入れると Rosetta で非常に遅い ③quarantine 属性による検査。docs/MAC.md に記載。asar:true も明示

## 5.6.0 での変更
- PC版にも使い方モーダル（helpOpen）。ツールバーの「使い方」ボタンから。配布時に説明書がなくても操作が分かるようにするため
- 配布用: `配布テンプレート/はじめにお読みください.txt`（アプリと同梱する説明書）と `docs/DISTRIBUTE.md`（作りかた・渡しかた）

## 5.7.0 での変更
- 取り込みプレビュー: cloudItems をそのまま貼らず、previewOpen の一覧でチェックしたものだけ placeIncoming する。貼ったものだけ cloudRemove。「貼らずに消す」もある
- スマホ版の送り先: projectList で共有中プロジェクトを取得し、target {id,name} を選ばせる（TARGET_KEY に保存）。送信データに targetProjectId/targetProjectName を載せ、PC側プレビューで現在のプロジェクト宛てを既定選択にする
- スマホ版の呼び名を「受信箱」→「アイデアボックス」に統一（manifest/titleも Idea Box）
- 修正: 送信済み案内のブロックが二重にあった／読み込み処理に catalog・areas の復元が無く、次回開いたときに初期値へ戻っていた

## 5.8.0 での変更
- タグ表のクラウド共有: `projects/{room}/items/__tags` に {presetTags, alertTags} を置く（ルール変更は不要。id が "__" で始まるものは projectList から除外している）
  - **クラウドが正**。起動時（cloud が入った時）に tagsPull して上書きし、編集後3秒で tagsPush
  - tagsLoaded ref で「読み終わる前に上げてしまう」事故を防ぐ。tagsSaved ref で同じ内容の再送を止める
  - スマホ版は ALERT_TAGS を定数から state（alertTags）へ変更。tagColorOfIn の第3引数で渡す

## 5.9.0 での変更
- **通し番号(num)は「今ある付箋だけ・空きなし」**にした。`renumber(notes)` が現在の num の順を保ったまま 1..N に詰め直し、`nextNum` を N+1 で返す純粋関数
  - 呼ぶのは2箇所だけ: `normalizeBoard`（読み込み時。既存データの飛び番号もここで解消）と `removeNoteIds`（剥がしたとき）
  - 追加は従来どおり `nextNum++`。削除のたびに詰めているので、それだけで空きなしが保たれる
- **本文の `#番号` も同じ対応表で書き換える**。`note.refs` はID参照なので無関係だが、本文の `#N` は描画時に num で照合している（`textRefIds`）ため、詰めるとそのままでは別の付箋を指してしまう
  - 置換は `replace(/#(\d+)/g, ...)` の**1パス**で行う。順に置換すると `#7→#5` と `#5→#4` が連鎖して壊れる
  - 対応表にない番号（消えた付箋への参照、`#999`、色コードの `#333333` など）は**触らない**。本文はユーザーが書いた文章なので勝手に消さない
  - 副作用: 「第3章」の意味で書いた `#3` も巻き込まれる。ユーザーに確認のうえ採用した仕様
- 剥がす処理を `removeNoteIds(doomed)` に一本化した（`removeNote` / `deleteSelected` / `listDelete` / `listMoveTo` の4箇所が同じ `.filter().map()` を複製していた）
  - 引用外し・線の削除・番号の詰め直しを**1回の updateBoard で**まとめて行う。別々に更新すると、詰める前の番号が一瞬描画される
- `clearAll` は `nextNum` も 1 に戻す（ここだけ `removeNoteIds` を通らないため）

## 5.10.0 での変更
- **スマホ版PWAが新しい版に入れ替わらない不具合を修正**（実ブラウザで再現・修正の両方を確認済み）
  - 原因は2つ重なっていた。①`mobile-sw.js` の navigate 分岐が**キャッシュ優先で取り直しが無い**（JS/CSS 側だけ stale-while-revalidate になっていた）。`mobile.html` はハッシュ付きのファイル名を持つので、HTMLが固定されると読み込むJSも永久に固定される。②`CACHE` が固定値で `mobile-sw.js` の中身が版ごとに変わらないため、ブラウザが再インストールせず `precache()` が二度と走らない
  - 直し方: navigate でも `e.waitUntil(update())` を回す。`update()` は**ページ本体だけ毎回取り直して比較**し（数KB）、変わっていたときだけ新しい JS/CSS を全部そろえ、**そろってから** `cache.put(PAGE)` する
  - **ページを先に差し替えてはいけない**。次にオフラインで開いたとき、新しいページがまだ保存していないJSを探して起動できなくなる
  - 古い版で固定された端末も、`mobile-sw.js` の中身が変わればブラウザが再インストールするので復帰する（SWスクリプト自体はキャッシュを迂回して取得されるため）。実験で確認済み
  - 版を重ねてもキャッシュが膨らまないよう、更新成功後に新しいHTMLが参照していないファイルを消す
- スマホ版のヘッダーに `版 {APP_VERSION}` を常時表示。⚙ と i の中にもあるが、開かないと見えなかった

## 6.0.0 での変更
- **保存先をサーバーに一本化**（ユーザー判断）。クラウドを設定していれば、すべてのプロジェクトをサーバーに置く。**サーバーが正・手元(localStorage)は控え**。クラウド未設定なら従来どおり端末内のみで、挙動は変わらない
  - 完全にサーバーのみにしなかった理由: 電波が無いと何もできなくなり、5.10.0 で直したオフライン起動と衝突するため。ユーザーに確認のうえ決定
  - 既定はすべて同期。プロジェクト単位で外せる（`cloudOptOut`、`idea-board-cloud-optout` に保存）。旧 `cloudShared` の state は廃止し、`cloudShared` は「同期中のIDの一覧」を返す派生値にした（UI の参照を変えないため）
- **分割保存**（`cloud.js`）: Firestore は1件あたり約1MB(バイト)まで。1プロジェクト=1件だと画像ですぐ超え、旧実装は**画像を捨てて**保存していた（一本化すると即データ消失になる）
  - `{projectId}` = 目次（name/device/savedAt/shape:"split"/meta）。`__b_{projectId}_{boardId}_{i}` = ボードの中身の i 番目のかけら（700KB ごと）
  - かけらは `__` で始まるので、既存の「`__` で始まるものは projectList から除外」でそのまま一覧から外れる。**Firestore のルールは変更不要**（`projects/{room}/items/{item}` が全部を覆う）
  - **大きさは文字数ではなくバイト数で測る**（`byteLen` / `splitByBytes`）。旧 `SIZE_LIMIT` は `body.length`（UTF-16の文字数）で判定しており、日本語だと実際の約3倍の大きさを「収まる」と誤判定していた
  - `splitByBytes` は `for...of` で code point 単位に進むので、サロゲートペア（絵文字など）を切らない
  - `projectPushSplit(conf, project, device, prev)` は **中身が変わったボードだけ**書く（`hashOf` の指紋で比較）。短くなったボードの余ったかけら、消えたボードのかけらも片づける
  - `projectPullSplit` は旧形式（`payload` に丸ごと）も読める。旧 `projectPush/projectPull/projectRemove/stripImages/SIZE_LIMIT` は削除した（残すと、呼んだときに黙って画像を捨てるため）
- **同期の判断**（`IdeaBoard.jsx` の `reconcile` / `pushOne` / `pullOne`）
  - `syncState[pid] = { h: 最後に同期した中身の指紋, at: そのときのサーバー savedAt }` を `idea-board-sync-state` に保存し、再起動をまたいで判断できるようにした
  - 手元だけ変わった → 送る / サーバーだけ新しい → 読む / **両方変わった → 競合。黙って上書きせず `syncConflict` で利用者に選ばせる**
  - この端末で一度も同期していないものは、サーバーにあればサーバーを正とする。サーバーに無ければ送る（初回の移行）
  - **起動直後の突き合わせ（`reconciled`）が終わるまでは送らない**。先に送ると、他の端末が更新したサーバーの中身を古い手元で上書きしてしまう
  - `pushOne` は送る直前に `projectHead`（目次の savedAt だけ、1回の読み取り）で確かめ、他の端末が先に書いていたら送らずに競合にする。確認と書き込みの間のわずかな時間の競合は防げないが、同じ人が複数端末を使う想定では十分
  - **サーバーで手元を置き換える前に必ず `makeBackup(..., "サーバーから読み込む前")`**。競合でどちらを選んでも、選ばなかった方はその端末の復元ポイントに残る（**6.0.0 では空き容量が足りないと黙って残らなかった。6.0.1 で修正**）
  - **白紙のプロジェクトは送らない**（`isBlank`）。新しい端末は起動時に空の「プロジェクト1」を作るため、送るとサーバーに空の同名プロジェクトが増えていく。端末が白紙しか持っておらずサーバーにプロジェクトがあれば、**サーバーの最新を開いて白紙は片づける**（新しい端末でHTMLを開いたら自分のデータが出る、が一本化の本題）
  - 変化が無ければ書かないので、2台を開きっぱなしにしても書き込みの応酬（ピンポン）は起きない（20秒放置で書き込み0件を確認）
  - 定期の突き合わせは60秒（パネル表示中10秒）。プロジェクトを開いたときにも行う
- **HTML 1枚の版**: `npm run single` → `release/IdeaBoard.html`（約300KB）。`vite.single.config.js` で1チャンクにまとめ、`tools/bundle-single.mjs` が JS を `<script type="module">` として HTML に埋め込み、manifest/アイコンの参照を外す
  - 通常の `dist/index.html` を file:// で開くと、別ファイルの module が CORS で止められて**白画面**になる（`from origin 'null' has been blocked`）。埋め込んだ module なら取りに行かないので動く
  - file:// でも localStorage は使え、開き直しても残る（Chromium で確認、Safari はユーザーが確認）。ただし file:// の保存先は**すべてのローカルHTMLで共通**
  - 同期フォルダ（OneDrive）は Electron 専用なので HTML 版には無い
- 検証: 偽の Firestore（REST の形だけ本物と同じ・1MB超は拒否）を立て、実ブラウザ2台（http と file:// の両方）で 移行 / 別端末で開く / 編集の伝達 / 競合で上書きしない / 両方の選択肢で復元ポイントに残る / オフライン編集の後送り / 1MB超の画像 を確認。**本物の Firestore には当てていない**

## 6.0.1 での変更
- **消失経路の修正（実ブラウザで再現→修正を確認）**: 空き容量が一杯の端末で、競合時に「サーバーの方」を選ぶと、手元の内容が現在の中身・控え・サーバーのどこにも残らず、知らせも出なかった
  - 原因: `writeBackups` は容量に入らないと古い控えから捨てて再試行し、最後は**何も書かずに正常終了**する。`pullOne` は `makeBackup(...).catch(() => {})` で結果を見ずに上書きへ進んでいた
  - `makeBackup` は書いたあと `loadBackups` で**読み戻して確かめ**、残せたかを返す（writeBackups の戻り値は信用しない）。`lastBackupAt` は残せたときだけ更新
  - `pullOne` は `buildPulled` で組み立ててから、手元と中身が違えば控えを確かめ、**残せなければ置き換えない**（`keepFail` を立てて画面上部で知らせ、そのプロジェクトを `exportProject(target)` で書き出せるボタンを出す）
  - **「手元は前回同期したまま」でも控えを省いてはいけない**。他の端末が競合で「この端末の方」を選ぶとサーバー上の同じ版は上書きされ、その版はこの端末の手元にしか無いことがある。中身が同一のときだけ省く（容量一杯でも、失うもののない同期は止めないため）
  - `restoreBackup`（「復元前」）と「今の状態を残す」も、控えを確かめられなければ止めて知らせる
  - `exportProject` に対象を渡せるようにした。**`onClick={exportProject}` と書くとクリックイベントが対象として渡って壊れる**ので `onClick={() => exportProject()}` にしている
- 6.0.0 で画面に足した説明文（サーバー保存の説明、「選ばなかった方も残ります」）を外した（上記のユーザー指定）
- **残っている消失経路（次に対処する。ユーザーと合意済みの順）**。目標は「アプリが利用者の知らないうちに書いたものを消すことはない」を、控えの確認と組み合わせ試験で根拠を持って言えるようにすること
  1. 保存失敗の知らせが6秒で消える（容量一杯のとき編集はメモリにしか無く、閉じると消える）→ 解決するまで出し続ける
  2. 更新時刻が各端末の時計（`Date.now()`）。2秒以上ずれると新旧を取り違えて黙って上書きしうる → Firestore のサーバー時刻（updateTime）と条件付き書き込み（`currentDocument.updateTime`）に置き換える。確認→書き込みの隙間の競合もこれで無くなる（本物の Firestore で要確認）
  3. 送信前に閉じる／ブラウザが消す → 未保存・未送信の常時表示、閉じる前の警告（beforeunload）、`navigator.storage.persist()`
  4. 控えは6件で押し出される（「サーバーから読み込む前」も約1時間の編集で消える）→ 上書き前の版をサーバーに履歴として残す（端末の容量に左右されない）
  5. 白紙判定で、名前だけ変えた空のプロジェクトが新しい端末で消える → 判定を厳しくする
  6. 検証: 2〜3台で編集・オフライン・容量一杯・時計のずれ・同時送信をランダムに組み合わせ、毎回「書かれたすべての版が、今の中身・サーバーの履歴・控えのどこかから取り出せる」ことを確かめる試験を作る。手で選んだ場面の試験では 6.0.0 の穴をすり抜けたため
- 別件（消失経路ではない）: index.html の起動エラー画面の「保存内容を初期化」は、旧キー `idea-board-state` しか消さず、今のデータには効かない。文言は「内容は消えます」のまま

## 6.0.2 での変更
- **消失経路の修正（実ブラウザで再現→修正を確認）**: サーバーの文書が100件を超えると、古い端末で**開き直しただけで**他の端末の最新がサーバーから上書きされていた（確認も知らせも無し）
  - 原因は3つの重なり。①`projectList` が `pageSize=100` で打ち切り（かけら `__b_…` は名前順で先頭に来るので、プロジェクト本体がこぼれる）②失敗しても `[]` を返す（「サーバーに何も無い」と取り違える）③`reconcile` の「サーバーに無い」分岐と、編集後の送信（`!known` のとき）が `pushOne(p, true)` で**確かめずに上書き**していた
  - `projectList` は `nextPageToken` で**最後のページまで**読み、失敗は**例外**にする（`refreshCloudProjects` が null を返し、reconcile はその回を中断する）
  - **確かめずに送る（force）のは2つだけ**: 利用者が競合で「この端末の方」を選んだとき（`keepLocalVersion`）と、直前に `projectHead` でサーバーに無いと確かめたとき（開いていないプロジェクトの移行）。それ以外は必ず `projectHead` を見る
  - `pushOne` は「この端末で一度も同期していないのにサーバーにある」も競合にする（どちらが新しいか分からないため）。6.0.1 で置き換えを止めた（`keepFail`）プロジェクトは `!known` のまま残るので、これが無いと次の編集で上書きしていた
- **読み取り回数**: 一覧を最後まで読むと、かけらの数だけ読み取りが増える。定期の確認（60秒／パネル表示中10秒）は `reconcile(false)` で**開いているプロジェクトの目次だけ**を `projectHead` で確かめる。全件の一覧（`reconcile(true)`）は起動時・プロジェクトを開いたとき・パネルを開いたときだけ。旧来の「60秒ごとに一覧」は、文書60件で1日約8.6万回となり、Firestore 無料枠（5万回/日）を超えていた
- 検証スクリプトの偽 Firestore は、一覧を本物と同じく**文書名の順・pageSize ごとのページ**で返す（6.0.0 の偽物は全件を一度に返しており、この穴を隠していた）
- **残っている消失経路**（6.0.1 の一覧に追加）
  - かけらを**同じ名前に上書き**している（`__b_{pid}_{bid}_{i}`）。複数かけらのボードを送る途中で切れると、目次は古い版のまま中身だけ新旧が混ざり、そのボードはサーバーから読めなくなる。同時に送った2台のかけらも混ざりうる → かけらに版の印を付けて新しい名前で書き、目次を条件付きで書き換えてから古いかけらを消す（書き込みの原子性）。形式の変更なので、旧形式の読み込みを残したうえで行う

## 6.1.0 での変更
- 対処の範囲（ユーザー判断）: 6.0.2 時点の残り経路のうち「保存失敗の知らせが6秒で消える」「閉じる直前の編集・ブラウザがデータを消す」「控えが6件で押し出される」は**対処しない**（利用者のストレスにならないため）。対処したのは下の3つ
- **仕様の確認**: Firestore の discovery 文書（`https://firestore.googleapis.com/$discovery/rest?version=v1`、revision 20260911）で確認した。`documents.patch` の `currentDocument.updateTime`（その時刻に最後に更新された文書だけ書く）と `currentDocument.exists=false`（無いときだけ書く）、`Document.updateTime`（書くたびに単調に増えるサーバー時刻。patch の応答にも入る）、`documents.list` の既定の並び（`__name__` 昇順）と「pageSize より少なく返すことがある」。firebase.google.com は作業環境から見られないが、discovery 文書と `firestore.googleapis.com` には届く
- **書き込みの原子性**（実際に壊れることを再現→修正を確認）: 6.0.2 までは、かけらを同じ名前（`__b_{pid}_{bid}_{i}`）に上書きしていた。送信が途中で切れると新旧のかけらが混ざり、**エラーも出ずに「どちらの版でもない」中身が読めた**（長さが同じだと JSON としては正しくなってしまう。画像が半分だけ新しいなど）。2台の同時送信でも300回中38回混ざった
  - かけらは毎回**新しい名前** `__b_{pid}_{bid}_{v}_{i}`（`v` は書き込みごとの版の印）に `currentDocument.exists=false` で書く。目次（`meta.boards[].v`）を書き換えたときに初めて切り替わり、そのあとで古いかけらを消す。途中で切れたら書いたかけらを片づける。どこで切れても目次はどちらかの版を丸ごと指す
  - `v` の無い旧形式のかけらも読める。新しい版を書くと旧形式のかけらは片づく
  - 目次を読んでからかけらを読むまでに他の端末が切り替えると古いかけらが消えていることがある。`projectPullSplit` は目次から読み直す（最大3回。混ぜない）
- **新旧はサーバーの時刻で判断**（時計のずれで黙って上書きすることを実ブラウザで再現→修正を確認）: 6.0.2 までは `savedAt`（端末の時計）の大小で比べていた。時計が遅れた端末の新しい編集を「古い」と取り違え、他の端末が黙って上書きしていた
  - `syncState[pid].ut` にサーバーの `updateTime` を持ち、`sameVersion` は**一致するか**だけを見る（大小は比べない）。6.0.2 までの記録（`ut` 無し）は `savedAt` が**同じ値か**で比べる
  - **目次の書き換えは必ず条件つき**（`pushOne` の mode: `check` は「サーバーが最後に同期した版のまま」なら `currentDocument.updateTime`、`absent` は `exists=false`、`override`＝「この端末の方」は今見えているサーバーの版の `updateTime`）。確かめてから書くまでに他の端末が書いても、条件が合わず書き換わらない → 競合として尋ねる。2台同時でも必ず片方だけが書ける
  - `savedAt` は表示用にだけ残す（一覧の並びも `updateTime` で並べる）
- **「この端末の方」でサーバーの版が壊れる不具合**（読み直しで発見→直す前に壊れることを確認→修正）: 変わっていないボードは前回の記録（`pushHashes`）のかけらを使い回すが、競合中は他の端末がそのかけらを消していることがある。**使い回してよいのは、記録がサーバーの今の版と同じ（`prev.__ut === expect.updateTime`）ときだけ**。違えば目次から読み直す。`exists=false`（サーバーに無い）のときは全部書く
- 「この端末の方」を選んで送れなかったとき（入れ違い・電波が無い）、競合の案内を消さない（消すと保存されたと誤解させる）
- **白紙判定**: 名前が既定（`/^プロジェクト\d*$/`）で、ボードも既定の名前（`/^ボード\d*$/`）1枚・中身が空のときだけ白紙と見なす。名前を付けただけの空のプロジェクトは片づけない（新しい端末で消えていた）
- **検証の道具**（作業用。リポジトリには入れていない）
  - 偽 Firestore は discovery 文書の仕様どおり（`updateTime` の単調増加、条件付き書き込み、1MiB 上限、`__name__` 順のページ分け、書き込み途中の通信断の注入）
  - 組み合わせの試験: 3台（時計 0・−7分・+3分）で、編集・待つ・開き直す・オフライン・競合での選択（容量一杯を含む）を乱数で30手。最後に「書いた文が、どの端末の**今の中身**か、**サーバーの今の版**にあるか」を確かめる。消えてよいのは、利用者が見たうえで書き換えたものと、競合の案内で利用者がもう一方を選んで捨てたものだけ。**復元ポイントにあるだけでは合格にしない**（利用者から見れば「いつの間にか消えている」ため）
  - この試験を作る途中で、判定側の誤りを2つ直した（復元ポイントの中身が二重の JSON で検索できていなかった／知らせを閉じるつもりで付箋の ✕（剥がす）を押していた）。**直す前の版にかけて消失を検出できることを確かめてから**修正版にかけた
  - 結果: 集計中（6.1.0 のコミット時点）。決まった手順の試験（時計のずれ・2台同期8場面・容量一杯・100件超と一覧の失敗・白紙判定）と単体試験（分割保存・原子性・上書き時の使い回し・番号詰め）はすべて通過
